// Largest of Two Numbers   
#include<stdio.h>
int main()
{
    int a,b;
    printf("enter your number a:");
    scanf("%d", &a);
    printf("enter your number b:");
    scanf("%d", &b);
    if (a>b)
    printf("number a is bigger than number b");
    else
    printf("number b is bigger than a");
    return 0;

}